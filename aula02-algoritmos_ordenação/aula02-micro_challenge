numeros = [5, 3, 8, 2]

def bubble_sort(lista):
    n = len(lista)
    trocasb = 0

    for i in range(n):
        for j in range (n - 1 - i):
            if lista[j] > lista[j + 1]:
                lista[j], lista[j + 1] = lista[j + 1], lista[j]
                trocasb += 1

    return lista, trocasb

def insertion_sort(lista):
    comparacoes, deslocamentos = 0
    for i in range(1, len(lista)):
        atual = lista[i]
        j = i - 1
        while j >=0: 
            comparacoes += 1
            if lista[j] > atual:
                lista[j + 1] = lista[j]
                j -= 1
                deslocamentos += 1
            else:
                break

        lista[j + 1] = atual

    return lista, comparacoes, deslocamentos

def selection_sort(lista):
    n = len(lista)
    trocasS = 0

    for i in range(n):
        menor = i
        for j in range(i + 1, n):
            if lista[j] < lista[menor]:
                menor = j

        lista[i], lista[menor] = lista[menor], lista[i]
        trocasS += 1

    return lista, trocasS

bubble, comp_b, trocas_b = bubble_sort(numeros[:])
insertion, comp_i, trocas_i = insertion_sort(numeros[:])
selection, comp_s, trocas_s = selection_sort(numeros[:])

numeros = [5, 3, 8, 2]
print("Bubble Sort:", trocas_b, comp_b)

numeros = [5, 3, 8, 2]
print("Insertion Sort:", trocas_i, comp_i)

numeros = [5, 3, 8, 2]
print("Selection Sort:", trocas_s, comp_s)
